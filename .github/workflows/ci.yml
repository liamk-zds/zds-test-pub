# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "dev" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

env:
  GAR_LOCATION: 'australia-southeast1'
  #PROJECT_ID: 'zdsdatamatch00'
  #EPOSITORY: 'australia-southeast1-docker.pkg.dev/zdsdatamatch00/zds-demo-repo'

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"

  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    
    permissions:
      contents: 'read'
      id-token: 'write'
    
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4

      - id: 'auth'
        name: 'Authenticate to Google Cloud'
        uses: 'google-github-actions/auth@v2' # google-github-actions/auth
        with:
          project_id: 'zdsdatamatch00'
          workload_identity_provider: 'projects/427954152403/locations/global/workloadIdentityPools/github/providers/my-repo'
          #workload_identity_provider: 'projects/427954152403/locations/global/workloadIdentityPools/zds-pool/providers/my-provider'
          service_account: 'sv-git@zdsdatamatch00.iam.gserviceaccount.com'

      - name: Build and Push App
        run: |
          IMAGE_NAME='test'
          REPO_NAME='australia-southeast1-docker.pkg.dev/zdsdatamatch00/zds-demo-repo'
          gcloud auth configure-docker australia-southeast1-docker.pkg.dev
          docker build --tag ${IMAGE_NAME} .
          docker tag ${IMAGE_NAME} ${REPO_NAME}${IMAGE_NAME}
          docker push {REPO_NAME}${IMAGE_NAME}
          
          #docker build --tag ${IMAGE_NAME} --file src/Dockerfile .
          #docker tag ${IMAGE_NAME} ${REPO_NAME}${IMAGE_NAME}
          #echo ${REPO_NAME}${IMAGE_NAME}
          #docker push ${REPO_NAME}${IMAGE_NAME}
      
      - name: Build and Push DAG


      #- name: 'Set up gcloud SDK'
      #  uses: 'google-github-actions/setup-gcloud@v1'

#      - name: Build and Push Docker Image
#        uses: mr-smithers-excellent/docker-build-push@v4
#        with:
#          image: liamkzds/demo-app
#          registry: docker.io
#          username: ${{ secrets.DOCKER_USERNAME }}
#          password: ${{ secrets.DOCKER_PASSWORD }}

#      - name: 'Docker Auth'
#        uses: 'docker/login-action@9780b0c442fbb1117ed29e0efdff1e18412f7567' # docker/login-action@v3
#        with:
#          username: 'oauth2accesstoken'
#          password: '${{ steps.auth.outputs.auth_token }}'
#          registry: '${{ env.GAR_LOCATION }}-docker.pkg.dev'  

#    # Build the Docker image
#      - name: 'Build and push Docker container'
#        run: |-
#          DOCKER_TAG="${GAR_LOCATION}-docker.pkg.dev/${PROJECT_ID}/${REPOSITORY}/${IMAGE}:${GITHUB_SHA}"
#
#          docker build \
#            --tag "${DOCKER_TAG}" \
#            --build-arg GITHUB_SHA="${GITHUB_SHA}" \
#            --build-arg GITHUB_REF="${GITHUB_REF}" \  